{
    "version": 2,
    "name": "miracle",
    "builds": [{ "src": "miracle/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "miracle/$1" }
    ]
}